[project]
name = "openrouter-demos"
version = "0.1.0"
description = "Four RouteKit demos on OpenRouter: support deflection, GTM motion, provider bakeoff, Caesar debate"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "httpx==0.28.1",
  "pytest==8.4.2",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"
filterwarnings = ["ignore::DeprecationWarning"]
